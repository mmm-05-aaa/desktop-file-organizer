# 参与开发

请先在隔离演示目录运行，使用 Windows 和 Python 3.10+。基础测试仅需标准库与 Tkinter：

```powershell
python -B -m unittest discover -s tests -v
```

修复应先增加能复现问题的失败测试，再修改实现并运行全部测试。文件操作测试必须同时隔离输入目录和状态目录，不能扫描、移动、撤销真实桌面的文件。故障测试须核对源/目标内容、事务状态和恢复结果，不能只看退出码。

不要在反馈、提交、截图中包含用户文件名、路径、事务日志、令牌或其他私人数据。报告问题请提供合成最小复现、Windows/Python版本及脱敏错误信息。

修改事务、撤销、路径约束、Web授权或发布流程时，应说明安全边界与中断行为。未经验证的支持范围不要写进 README。
