# 截图说明

当前真实界面截图：`screenshots/preview.png`。

截图使用公共目录中的临时合成样本，没有读取或展示用户实际桌面。窗口及列宽经过调整，以完整显示文件名、目标路径、规则匹配分与 PDF 警告；它不是设计稿，也没有伪造整理结果。

## 重拍原则

1. 只使用隔离合成文件和隔离状态，先确认界面顶部不是用户真实桌面。
2. 演示数据缺失时可运行 `reset_demo.bat` 补齐；它不会删除已修改或未知文件，整理过的数据优先通过软件撤销。
3. 展开网页、文本和相近文件组；异常 PDF 应显示警告并被排除。
4. 不把规则匹配分描述为准确率或统计概率。
5. 截取应用客户区，避免露出聊天、桌面或其他窗口；高 DPI 环境核对截图的物理像素坐标。
6. 展示确认流程时，使用“执行安全项目”及“撤销 / 恢复上次整理”，只操作合成样本。

仅 `preview.png` 已制作并验收；没有把尚未拍摄的确认框或完成结果列作已有截图。
