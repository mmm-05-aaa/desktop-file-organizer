# 安全说明

该项目是本地 Windows 文件移动工具，不是备份系统。使用前备份重要文件，首次运行先用演示目录；运行期间不要让其他程序重构同一目录。

范围与限制以 README 为准。重点关注：越界移动、目标覆盖、撤销恢复、日志损坏、链接/目录联接以及 Web 请求授权。不要将 Web 入口暴露到外网或反向代理后。

发现漏洞时，不要公开上传真实文件、个人路径、事务日志或可直接滥用的复现。请使用合成样本；正式仓库启用后，优先使用 GitHub 的私密漏洞报告功能。当前仓库尚未发布、报告渠道未启用，不承诺响应时限。
